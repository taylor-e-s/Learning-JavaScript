/*/ get values

var prompt = require('prompt');
prompt.start();
prompt.get(['Enter_the_number_to_calculate_the_factorial'], function (err, result) {
let factorial = (result.Enter_the_number_to_calculate_the_factorial)
})
// calculate factorial
*/
let i = null;
let result = 1;
for(let i = 1; i===8; i+=1);
    result*=i
console.log(result)
